pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Backend API for the interactive prototype (Step 5 / deliverable) --
# see backend/ and frontend/ (React). Run with: uvicorn backend.main:app
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31

# Optional, used only if you want to work in notebooks alongside the scripts
jupyter>=1.0
